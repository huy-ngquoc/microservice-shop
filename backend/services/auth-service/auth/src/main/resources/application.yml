spring:
    application:
        name: auth-service
    config:
        import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"
    cloud:
        config:
            fail-fast: true
            retry:
                max-attempts: 5
    mongodb:
        uri: mongodb://alibou:alibou@localhost:27020/auth_db?authSource=admin&uuidRepresentation=STANDARD
