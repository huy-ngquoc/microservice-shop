server:
    port: 8070

spring:
    output:
        ansi:
            enabled: ALWAYS
    mongodb:
        host: localhost
        port: 27017
        database: product
        username: alibou
        password: alibou
        authentication-database: admin
        representation:
            uuid: STANDARD 
    servlet:
        multipart:
            max-file-size: 5MB
            max-request-size: 6MB


logging:
    level:
        "[vn.edu.uit.msshop]": DEBUG
        org:
            springframework:
                data:
                    mongodb:
                        core:
                            MongoTemplate: DEBUG