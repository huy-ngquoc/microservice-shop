spring:
    application:
        name: inventory-service
    config:
        import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"
    cloud:
        config:
            fail-fast: true
            retry:
                max-attempts: 5
